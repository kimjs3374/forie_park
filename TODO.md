# 포리에 방문차량 예약 — 로드맵 / 결정 대기

## ✅ 구현 완료
- 입주민 가입(관리사무소 승인제) / 로그인·로그아웃
- 아이디 찾기(동/호/이름/연락처), 비번 찾기(+아이디→임시비번), **첫 로그인 강제 비번변경**
- 방문차량 등록·조회·취소 (차량번호 형식검증, 1회 최대 72시간)
- 관리자: 가입 승인/반려, 전체 방문등록 조회(기간필터)·CSV, 대시보드
- **CSRF 보호** (전 POST 폼)
- **신규 가입 텔레그램 알림** (봇 "포리에 방문차량")
- Supabase REST 백엔드 (`parking_` 프리픽스 테이블), Cloudflare 뒤 HTTPS

## 🟡 정책 확정 후 구현 (← 회의 안건)

### 중복 정책
- **① 같은 차량번호 중복 등록**: 시간 겹치면 차단(추천) / 활성 1건만 / 제한없음
- **② 다른 세대가 같은 차 등록**: 허용(추천) / 차단

### 정원 정책
- **③ 세대당 동시 등록 대수**: 무제한 / 2~3대(추천) / 1대
- **④ 단지 전체 방문주차 정원** ← 핵심:
  - (a) 상한 없음(기록만) — 방문주차 넉넉할 때
  - (b) 하드 상한 N면 — 같은 시간대 N건 초과 시 "만차" 거부
  - (c) 시간대별 상한
  - ※ (b)(c)는 "특정 시각 겹치는 등록 수 계산" 로직 필요 → 난이도 상승
  - **결정 필요: 방문주차 면수 / 평소 부족 여부**

### 곁다리
- 1회 최대 체류시간: 72h 유지 / 24~48h 단축
- 사전 등록 한도(며칠 전까지): 필요 여부

## ⚪ 나중에 (외부 규격/여유 시)
- **nexpa(주차관제) 연동** — 방향 A(자동: 등록차 자동 인식→차단기). 규격 확정 시 `app/nexpa_adapter.py`의 send/cancel 두 함수만 구현하면 됨(격리 완료)
- 자동 만료 / 상태표시(입차예정·이용중·종료) — nexpa 실제 출차신호 반영 여부와 함께
- 관리사무소의 방문 취소/수정, 세대·입주민 관리
- 개인정보 자동 파기, 가입/등록 레이트리밋

## 참고
- 대금청구 없음 → 시간차감/정산(`monthly_free_minutes`,`used_minutes`) 로직 불필요(스키마에만 존재)
